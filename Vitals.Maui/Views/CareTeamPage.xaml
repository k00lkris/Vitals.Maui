<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Vitals.Maui.ViewModels"
             xmlns:models="clr-namespace:Vitals.Maui.Models"
             x:Class="Vitals.Maui.Views.CareTeamPage"
             x:DataType="vm:CareTeamViewModel"
             BackgroundColor="{DynamicResource PageBackground}">

    <RefreshView Command="{Binding LoadCommand}"
                 IsRefreshing="{Binding IsBusy}"
                 RefreshColor="{DynamicResource PrimaryAccent}">
        <ScrollView>
            <VerticalStackLayout Padding="16" Spacing="12">

                <!-- Header Card -->
                <Border Padding="16"
                        BackgroundColor="{DynamicResource CardBackground}"
                        StrokeShape="RoundRectangle 12"
                        Stroke="{DynamicResource CardStroke}">
                    <VerticalStackLayout Spacing="10">
                        <Label FontSize="18"
                               FontAttributes="Bold"
                               TextColor="{DynamicResource TextPrimary}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Care Team for " TextColor="{DynamicResource PrimaryAccent}" />
                                    <Span Text="{Binding SelectedPatient.FullName}"
                                          TextColor="{DynamicResource TextPrimary}"
                                          FontAttributes="Bold" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                        <Button Text="➕  Add Provider"
                                BackgroundColor="{DynamicResource ButtonBackground}"
                                TextColor="{DynamicResource TextPrimary}"
                                FontSize="13"
                                HeightRequest="44"
                                CornerRadius="8"
                                Command="{Binding OpenAddDoctorCommand}" />
                    </VerticalStackLayout>
                </Border>

                <!-- No Doctors -->
                <Label Text="No providers on record."
                       IsVisible="{Binding HasNoDoctors}"
                       FontSize="14"
                       TextColor="{DynamicResource TextMuted}"
                       HorizontalOptions="Center"
                       Margin="0,16" />

                <!-- Doctors List -->
                <CollectionView ItemsSource="{Binding Doctors}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Doctor">
                            <Border Padding="16"
                                    Margin="0,0,0,8"
                                    BackgroundColor="{DynamicResource CardBackground}"
                                    StrokeShape="RoundRectangle 12"
                                    Stroke="{DynamicResource CardStroke}">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CareTeamViewModel}},
                                            Path=OpenDoctorDetailCommand}"
                                        CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>
                                <Grid ColumnDefinitions="*,Auto">
                                    <VerticalStackLayout Grid.Column="0" Spacing="4">
                                        <Label Text="{Binding Name}"
                                               FontSize="16"
                                               FontAttributes="Bold"
                                               TextColor="{DynamicResource TextPrimary}"
                                               LineBreakMode="WordWrap" />
                                        <Label Text="{Binding Specialty}"
                                               FontSize="13"
                                               FontAttributes="Italic"
                                               TextColor="{DynamicResource TextMuted}" />
                                    </VerticalStackLayout>
                                    <Border Grid.Column="1"
                                            Padding="8,4"
                                            BackgroundColor="{DynamicResource ButtonBackground}"
                                            StrokeShape="RoundRectangle 6"
                                            VerticalOptions="Center"
                                            IsVisible="{Binding IsPrimary}">
                                        <Label Text="🩺 PCP"
                                               FontSize="11"
                                               FontAttributes="Bold"
                                               TextColor="{DynamicResource TextPrimary}" />
                                    </Border>
                                    <Label Grid.Column="1"
                                           Text="›"
                                           FontSize="22"
                                           TextColor="{DynamicResource PrimaryAccent}"
                                           VerticalOptions="Center"
                                           IsVisible="{Binding IsPrimary,
                                               Converter={StaticResource InvertedBoolConverter}}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Status Message -->
                <Label Text="{Binding StatusMessage}"
                       IsVisible="{Binding StatusMessage,
                           Converter={StaticResource StringToBoolConverter}}"
                       TextColor="#d32f2f"
                       FontSize="13"
                       HorizontalOptions="Center" />

            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage>
