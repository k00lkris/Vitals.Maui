<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Vitals.Maui.ViewModels"
             x:Class="Vitals.Maui.Views.OnboardingVitalPreferencesPage"
             x:DataType="vm:OnboardingVitalPreferencesViewModel"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{DynamicResource PageBackground}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <!-- Back -->
            <Button Text="←"
                    Command="{Binding BackCommand}"
                    BackgroundColor="Transparent"
                    TextColor="{DynamicResource TextSecondary}"
                    FontSize="22"
                    HorizontalOptions="Start"
                    WidthRequest="44"
                    HeightRequest="44"
                    Padding="0" />

            <Label Text="What do you want to track?"
                   FontSize="26"
                   FontAttributes="Bold"
                   TextColor="{DynamicResource TextPrimary}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />

            <Label Text="Blood pressure is always tracked. Choose anything else that's relevant — you can change this anytime in Settings."
                   FontSize="14"
                   TextColor="{DynamicResource TextSecondary}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   Margin="16,0,16,8" />

            <Border Padding="16"
                    BackgroundColor="{DynamicResource CardBackground}"
                    StrokeShape="RoundRectangle 12"
                    Stroke="{DynamicResource CardStroke}">
                <VerticalStackLayout Spacing="0">

                    <!-- Blood Pressure — always on, no toggle -->
                    <Grid Padding="4,12" ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                            <Label Text="Blood Pressure" FontSize="15" TextColor="{DynamicResource TextPrimary}" />
                            <Label Text="Always tracked" FontSize="12" TextColor="{DynamicResource TextMuted}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1" Text="✓" FontSize="18" FontAttributes="Bold"
                               TextColor="{DynamicResource PrimaryAccent}" VerticalOptions="Center" />
                    </Grid>
                    <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />

                    <Grid Padding="4,12" ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="Heart Rate" FontSize="15" TextColor="{DynamicResource TextPrimary}" VerticalOptions="Center" />
                        <Switch Grid.Column="1" IsToggled="{Binding ShowHeartRate}" OnColor="{DynamicResource PrimaryAccent}" />
                    </Grid>
                    <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />

                    <Grid Padding="4,12" ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="SpO₂" FontSize="15" TextColor="{DynamicResource TextPrimary}" VerticalOptions="Center" />
                        <Switch Grid.Column="1" IsToggled="{Binding ShowSpo2}" OnColor="{DynamicResource PrimaryAccent}" />
                    </Grid>
                    <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />

                    <Grid Padding="4,12" ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="Temperature" FontSize="15" TextColor="{DynamicResource TextPrimary}" VerticalOptions="Center" />
                        <Switch Grid.Column="1" IsToggled="{Binding ShowTemperature}" OnColor="{DynamicResource PrimaryAccent}" />
                    </Grid>
                    <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />

                    <Grid Padding="4,12" ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="Weight" FontSize="15" TextColor="{DynamicResource TextPrimary}" VerticalOptions="Center" />
                        <Switch Grid.Column="1" IsToggled="{Binding ShowWeight}" OnColor="{DynamicResource PrimaryAccent}" />
                    </Grid>
                    <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />

                    <Grid Padding="4,12" ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="Blood Glucose" FontSize="15" TextColor="{DynamicResource TextPrimary}" VerticalOptions="Center" />
                        <Switch Grid.Column="1" IsToggled="{Binding ShowGlucose}" OnColor="{DynamicResource PrimaryAccent}" />
                    </Grid>

                </VerticalStackLayout>
            </Border>

            <!-- Progress dots (5 of 6) -->
            <HorizontalStackLayout Spacing="8" HorizontalOptions="Center" Margin="0,8,0,0">
                <BoxView HeightRequest="8" WidthRequest="8" CornerRadius="4" BackgroundColor="{DynamicResource DividerColor}" />
                <BoxView HeightRequest="8" WidthRequest="8" CornerRadius="4" BackgroundColor="{DynamicResource DividerColor}" />
                <BoxView HeightRequest="8" WidthRequest="8" CornerRadius="4" BackgroundColor="{DynamicResource DividerColor}" />
                <BoxView HeightRequest="8" WidthRequest="8" CornerRadius="4" BackgroundColor="{DynamicResource DividerColor}" />
                <BoxView HeightRequest="8" WidthRequest="24" CornerRadius="4" BackgroundColor="{DynamicResource PrimaryAccent}" />
                <BoxView HeightRequest="8" WidthRequest="8" CornerRadius="4" BackgroundColor="{DynamicResource DividerColor}" />
            </HorizontalStackLayout>

            <Button Text="Continue"
                    Command="{Binding ContinueCommand}"
                    BackgroundColor="{DynamicResource ButtonBackground}"
                    TextColor="{DynamicResource TextPrimary}"
                    FontSize="16"
                    FontAttributes="Bold"
                    HeightRequest="56"
                    CornerRadius="12"
                    Margin="0,4,0,0" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
