<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Vitals.Maui.ViewModels"
             x:Class="Vitals.Maui.Views.OnboardingPatientSetupPage"
             x:DataType="vm:OnboardingPatientSetupViewModel"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{DynamicResource PageBackground}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <!-- Back -->
            <Button Text="←"
                    Command="{Binding BackCommand}"
                    BackgroundColor="Transparent"
                    TextColor="{DynamicResource TextSecondary}"
                    FontSize="22"
                    HorizontalOptions="Start"
                    WidthRequest="44"
                    HeightRequest="44"
                    Padding="0" />

            <Label Text="{Binding Title}"
                   FontSize="26"
                   FontAttributes="Bold"
                   TextColor="{DynamicResource TextPrimary}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />

            <Label Text="{Binding Subtitle}"
                   FontSize="14"
                   TextColor="{DynamicResource TextSecondary}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   Margin="16,0,16,8" />

            <!-- Form -->
            <Border Padding="16"
                    BackgroundColor="{DynamicResource CardBackground}"
                    StrokeShape="RoundRectangle 12"
                    Stroke="{DynamicResource CardStroke}">
                <VerticalStackLayout Spacing="14">

                    <VerticalStackLayout Spacing="4">
                        <Label Text="First name" FontSize="13" TextColor="{DynamicResource TextSecondary}" />
                        <Entry Text="{Binding FirstName}"
                               Placeholder="First name"
                               TextColor="{DynamicResource TextPrimary}"
                               PlaceholderColor="{DynamicResource TextMuted}"
                               BackgroundColor="Transparent" />
                        <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Last name" FontSize="13" TextColor="{DynamicResource TextSecondary}" />
                        <Entry Text="{Binding LastName}"
                               Placeholder="Last name"
                               TextColor="{DynamicResource TextPrimary}"
                               PlaceholderColor="{DynamicResource TextMuted}"
                               BackgroundColor="Transparent" />
                        <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Date of birth" FontSize="13" TextColor="{DynamicResource TextSecondary}" />
                        <DatePicker Date="{Binding Dob}"
                                    TextColor="{DynamicResource TextPrimary}"
                                    BackgroundColor="Transparent" />
                        <BoxView HeightRequest="1" BackgroundColor="{DynamicResource DividerColor}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Gender (optional)" FontSize="13" TextColor="{DynamicResource TextSecondary}" />
                        <Picker SelectedItem="{Binding Gender}"
                                TextColor="{DynamicResource TextPrimary}"
                                TitleColor="{DynamicResource TextMuted}"
                                Title="Select one"
                                BackgroundColor="Transparent">
                            <Picker.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>Female</x:String>
                                    <x:String>Male</x:String>
                                    <x:String>Prefer not to say</x:String>
                                </x:Array>
                            </Picker.ItemsSource>
                        </Picker>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Border>

            <Label Text="{Binding StatusMessage}"
                   IsVisible="{Binding StatusMessage, Converter={StaticResource StringToBoolConverter}}"
                   FontSize="13"
                   TextColor="#E57373"
                   HorizontalOptions="Center" />

            <Button Text="Continue"
                    Command="{Binding SaveCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                    BackgroundColor="{DynamicResource ButtonBackground}"
                    TextColor="{DynamicResource TextPrimary}"
                    FontSize="16"
                    FontAttributes="Bold"
                    HeightRequest="56"
                    CornerRadius="12"
                    Margin="0,8,0,0" />

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="{DynamicResource PrimaryAccent}"
                               HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
